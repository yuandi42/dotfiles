#!/usr/bin/sh

case $BLOCK_BUTTON in
    3) setsid -f "$TERMINAL" -e "$EDITOR" "$0";;
    4) light -A 5 && pkill -RTMIN+2 dwmblocks;;
    5) light -U 5 && pkill -RTMIN+2 dwmblocks;;
esac

lgt=$(light -G | cut -d . -f 1)
printf "^b#d79921^^c#282828^   ^d^^b#a89984^^c#282828^ $lgt%% ^d^"
