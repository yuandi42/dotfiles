#!/usr/bin/sh
# SIGNAL 5

case $BLOCK_BUTTON in
    1) "$TERMINAL" -e "sb-popup";;
    3) setsid -f "$TERMINAL" -e "$EDITOR" "$0" >/dev/null/ 2>&1 &;; 
esac
color=#8ec07c
update=$(paru -Qu | wc -l)
[ $update -eq 0 ] && sb-show $color ' ' '✓' && exit
sb-show $color ' ' "$update"
