#!/usr/bin/sh
# show systray below the bar when clicked.
# SIGNAL 13

case $BLOCK_BUTTON in
    1) toggle-tray;;
    3) setsid -f "$TERMINAL" -e "$EDITOR" "$0" >/dev/null 2>&1 &;;
esac

printf "^b#d65d0e^^c#282828^   ^d^"
