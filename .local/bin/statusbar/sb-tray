#!/bin/sh
# toggle systray below the bar when clicked.
# SIGNAL 13

case $BLOCK_BUTTON in
    1) toggle-tray;;
    3) setsid -f "$TERMINAL" -e "$EDITOR" "$0" >/dev/null 2>&1 &;;
esac

color=#d65d0e
sb-show $color 
